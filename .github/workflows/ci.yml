name: CI

on: push

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest

    steps:
    - name: Set up Ruby 2.6
      uses: actions/setup-ruby@v1
      with:
        ruby-version: 2.6.x

    - name: RSpec
      run: |
        bundle exec rake
